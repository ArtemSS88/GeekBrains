import java.util.Scanner;
import java.lang.Math;

class HomeWork3{
    public static final Scanner scanner = new Scanner(System.in);

   public static void main(String[] args) {
    RiddleOfNumber();
  }

  public static void RiddleOfNumber(){
    Random random = new Random();
    int a;
    int[] data = new int[10];
    	System.out.println("Guess the number from 0 to 9 that the computer guessed. You have three tries");
    for (int i = 0; i < 3; i++){
      System.out.println("Try " + (i+1) + " - ");
      data [n] = (int)(Math.random() * 10);
      a = in.nextInt();
      if (a < n) System.out.println("Your number is greater!");
      if (a > n) System.out.println("Your number is less!");
      if (a == n) {System.out.println("You guessed!");
      System.out.println("Repeat one more time? 1 – yes / 0 – no");
      a = in.nextInt();
      if (a == 1) RiddleOfNumber();
      else {System.out.println("Thanks for the game!");
      break;
      }
      }
  }
        System.out.println("You didn't guess");
        System.out.println("Repeat one more time? 1 – yes / 0 – no");
        a = in.nextInt();
        if (a == 1) RiddleOfNumber();
         else {System.out.println("Thanks for the game!");
      }
  }
}
